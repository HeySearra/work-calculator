# 打工人仪表盘 · 部署与重构方案（React 版 v3）

> **选型已锁定**：后端 **Python + FastAPI**，数据库 **SQLite**（预留 PostgreSQL），访问控制 **完整多用户账号（JWT + users 表 + 按 user_id 隔离）**。
> **本版变更**：前端由「保留 vanilla JS 最小改动」改为 **「React 组件化重构」**。
> 目标：把现有纯前端原型，重构成「FastAPI 服务 + SQLite + 多用户 + React 前端」的云端永久存储应用；保留全部功能与页面，架构清晰、易维护、易扩展。

---

## 1. 现状评估（要点）

- 前端：`index.html`(687) + `app.js`(1218)，vanilla JS，零构建零依赖，所有逻辑堆在一个文件（路由/渲染/计算/存储混写）。
- 存储：`localStorage['dqrd_v1']`，整对象 `S` 序列化；`load()/save()` 是**唯一**存储出口。
- 状态模型 `S`：`profile/payday/pension/fire/invest`（配置）、`punches`（每日打卡，量最大）、`accounts`、`weekly/monthly/yearly`（理财记录）、`holidays`、`trend`。
- 原型遗留：`appToday()` 硬编码 `2026-09-07`，生产需换真实服务器时间。
- **重构判断**：原 `app.js` 已不适合继续堆砌。改为 React 后，页面/组件/状态/计算清晰分层，热力图/卡片/弹窗可复用，多用户与后续功能扩展成本大幅降低。后端 API 与前端框架无关，已锁定部分不变。

---

## 2. 目标架构

```
┌──────────────────┐   HTTPS + JSON (Bearer Token)   ┌──────────────────────────┐
│  React 前端       │ ◄─────────────────────────────► │  FastAPI 服务 (uvicorn)   │
│  (Vite 构建)      │  GET/PUT /api/state             │  ├─ /api/auth/* 登录注册  │
│  Zustand 状态     │  POST /api/punch               │  ├─ /api/state 整对象读写  │
│  TanStack Query   │  POST /api/import              │  ├─ 业务路由 (需 token)    │
│  组件化页面/图表  │                                │  └─ 数据访问层 (SQLAlchemy)│
└──────────────────┘                                └───────────┬──────────────┘
                                                              ┌──┴──┐
                                                              ▼     ▼
                                                       SQLite 文件    （预留 PostgreSQL）
                                                       data/app.db
```

**技术栈**
- 后端：FastAPI + SQLAlchemy（ORM，SQLite↔PG 仅改连接串）+ Pydantic（校验）+ JWT（python-jose）+ 密码哈希（passlib[bcrypt]）
- 前端：**React 18 + Vite + TypeScript**（推荐 TS：数据模型/多用户状态复杂，类型安全收益大；如需纯 JS 也可，去掉类型即可）
- 状态：`Zustand`（轻量全局 store，镜像原 `S`）+ `TanStack Query`（服务端状态/缓存）
- 图表：热力图继续**手写 SVG 组件**（GitHub 风格日历，保持 icost 清爽风格与完全可控）；其余简单趋势图可用轻量方案或同手写

---

## 3. 目标目录结构

```
打工人仪表盘/
├── server/                          # 后端（同前，与前端框架无关）
│   ├── main.py             # FastAPI 入口：挂载路由 + 静态托管（prod 托管 React 构建产物）
│   ├── db.py               # SQLAlchemy engine / Session / Base
│   ├── models.py           # ORM：User, KV, Punch, InvestRecord
│   ├── schemas.py          # Pydantic 模型
│   ├── auth.py             # 注册/登录/当前用户依赖 (JWT)
│   ├── dal.py              # 按 user_id 读写 state/punch/invest
│   ├── seed.py             # 旧 localStorage JSON 迁移入库（一次性）
│   └── routers/ auth.py state.py punch.py invest.py io.py
├── web/                             # ★ React 前端（Vite 项目）
│   ├── index.html
│   ├── vite.config.ts      # 开发代理 /api → :8000；build 输出到 ../server/static
│   ├── tsconfig.json  package.json
│   └── src/
│       ├── main.tsx        # 入口
│       ├── App.tsx         # 路由 + 全局布局（Sidebar / TabBar）+ 登录守卫
│       ├── api/
│       │   ├── client.ts   # fetch 封装 + 自动带 Bearer token + 错误处理
│       │   ├── state.ts    # GET/PUT /api/state
│       │   ├── auth.ts     # 登录/注册/me
│       │   └── punch.ts invest.ts
│       ├── store/
│       │   ├── useApp.ts   # Zustand：全局 S（原 app.js 的 S + DEFAULT）
│       │   └── useAuth.ts  # 当前用户 / token（持久化 localStorage）
│       ├── hooks/
│       │   └── useNow.ts   # 实时时钟（替代全局 setInterval，驱动今日跳动）
│       ├── utils/
│       │   ├── format.ts   # fmt / fmtN
│       │   ├── date.ts     # todayKey / 周起始 / 月份列 / 工作日判定
│       │   └── calc.ts     # 薪资/日薪/养老金/ FIRE / 收益率（原 app.js 计算逻辑平移）
│       ├── components/
│       │   ├── layout/ Sidebar.tsx TabBar.tsx Card.tsx Metric.tsx
│       │   ├── ui/ Modal.tsx Toast.tsx Radio.tsx
│       │   └── chart/ Heatmap.tsx        # 通用热力图（双色：收入橙 / 考勤蓝）
│       │           CalendarHeatmap.tsx   # 月份标签 + 分隔线（GitHub 风格）
│       └── views/
│           ├── LoginView.tsx
│           ├── TodayView.tsx       # 实时薪资跳动、发工资倒计时、打卡
│           ├── HistoryView.tsx    # 双热力图 + 打卡弹窗 + 年份切换
│           ├── AssetsView.tsx
│           ├── PensionView.tsx
│           ├── FireView.tsx
│           ├── InvestView.tsx      # 理财能力成长档案 + 年化曲线
│           └── SettingsView.tsx    # 作息/薪资/未打卡模式/节假日
├── data/app.db             # SQLite（.gitignore）
├── requirements.txt        # fastapi uvicorn sqlalchemy python-jose passlib[bcrypt] python-multipart
├── .env.example            # PORT JWT_SECRET DATABASE_URL TZ=Asia/Shanghai
├── README.md
└── 打工人仪表盘-部署与重构方案.md
```

---

## 4. 数据模型（SQLite / SQLAlchemy，全部带 user_id 隔离）

| 表 | 字段 | 说明 |
|---|---|---|
| `users` | `id PK`, `username UNIQUE`, `password_hash`, `created_at` | 多用户账号 |
| `kv` | `user_id FK`, `key`, `value(JSON)`, PK(`user_id,key)` | 存 `profile/payday/pension/fire/invest/accounts/trend/holidays`，每用户一套 |
| `punches` | `user_id FK`, `date`, `in`, `out`, `leave`, `note`, PK(`user_id,date)` | 每日打卡，量最大，独立表便于按年统计 |
| `invest_records` | `user_id FK`, `period`, `kind('week'/'month'/'year')`, `value REAL`, `meta` | 理财记录 |
| `meta` | `user_id`, `key`, `value` | 版本号等 |

> 迁移：现有 `localStorage` 的 `S` 导出 JSON → `seed.py` 按字段写入对应用户。

---

## 5. API 设计（与前端框架无关，不变）

**认证**
- `POST /api/auth/register` `{username,password}` → `{token}`
- `POST /api/auth/login` `{username,password}` → `{token}`
- `GET  /api/auth/me`（需 token）→ 当前用户

**业务（均需 `Authorization: Bearer <token>`）**
- 阶段一：`GET/PUT /api/state`（整对象读写，兼容现有 `save`）、`GET/POST /api/export|import`
- 阶段二：`POST/DELETE /api/punch/:date`、`PUT /api/settings`、`GET/POST /api/invest/:kind`
- 所有写操作后端按 `current_user.id` 隔离

**前端调用方式（React）**：`api/client.ts` 统一封装 `fetch`，自动附加 token、统一错误；`TanStack Query` 管理 `GET` 缓存与失效，`PUT/POST` 后失效刷新；`useApp`(Zustand) 持有本地 `S` 镜像，乐观更新 UI。

---

## 6. 重构步骤（分阶段、可回退）

1. **后端骨架**：`db.py/models.py` 建表；`main.py` 起 FastAPI + `GET/PUT /api/state`（先用内存/单用户验证）。
2. **认证系统**：`auth.py` + `routers/auth.py`，注册/登录/JWT；`dal.py` 所有读写带 `user_id`。
3. **React 项目骨架**：Vite + TS 起 `web/`；`useApp`(Zustand) 镜像 `S`、`api/client` 调 `/api/state`；先跑通「登录 → 拉取 state → 渲染今日」最小闭环。
4. **视图平移**：把 `app.js` 各 `render*` 函数改写为 React 组件（Today/History/Assets/Pension/Fire/Invest/Settings），计算逻辑搬进 `utils/calc.ts`。
5. **组件化图表**：`Heatmap`/`CalendarHeatmap` 通用组件（双实例复用：收入橙 + 考勤蓝），打卡弹窗 `Modal`。
6. **去演示化**：`appToday()` → 真实服务器时间（UTC+8）；`loadDemo` 保留为可选演示开关。
7. **数据迁移**：`seed.py` 导入现有 localStorage 导出 JSON。
8. **细化 API + 部署**：单资源 API；`vite build` → 后端静态托管；uvicorn/gunicorn + Nginx + HTTPS。

---

## 7. 部署（自托管）

- 前端：`npm run build` 产出静态文件，由 FastAPI 在 `/` 托管（或 Nginx 直托 + `/api` 反代）
- 后端：`uvicorn server.main:app`（生产 `gunicorn -k uvicorn.workers.UvicornWorker`）
- 反代：Nginx `https://dash.yourdomain.com` → `localhost:8000`
- HTTPS：Let's Encrypt
- 备份：`data/app.db` 单文件定时 `cp`/`sqlite3 .dump`
- 最低配置：1 核 1G 即可（构建在本地/CI 完成，服务器只跑产物）

---

## 8. 收益与代价（vanilla 最小改动 vs React 重构）

| 维度 | vanilla 最小改动 | React 重构（本版） |
|---|---|---|
| 改动面 | 仅 `load/save` + 登录页 | 前端整体组件化重写 |
| 可维护性 | 中（仍单文件风险） | 高（分层清晰、组件复用） |
| 扩展性 | 中 | 高（多页/多用户/新功能易加） |
| 引入成本 | 零（无构建链） | Vite/Node 工具链 + 包体积 |
| 类型安全 | 无 | TS 可选，数据模型更稳 |
| 适合场景 | 个人自用、快速上线 | 长期维护、多人使用、持续迭代 |

> 结论：既然已确定做**多用户 + 服务器部署 + 长期维护**，React 重构的一次性成本，会在后续可维护性上持续回本。

---

## 9. 风险与注意

- **SQLite 并发**：多用户高频并发写会锁；当前够用，DAL 用 SQLAlchemy，日后换 PG 仅改 `DATABASE_URL`（已预留）。
- **构建产物托管**：生产建议 Nginx 直接托管 `web/dist`，`/api` 反代到 FastAPI，减轻 Python 静态文件压力。
- **旧数据安全**：开工前先用现有「导出 JSON」备份本地副本。
- **可回退**：后端 API 阶段一即对齐原 `save/load`，若 React 重写中途需暂停，后端 + 旧前端仍可独立运行。

---

## 10. 确认状态

- 后端：**Python + FastAPI** ✅
- 数据库：**SQLite**（预留 PG）✅
- 前端：**React 18 + Vite（+ TypeScript 推荐）组件化重构** ✅（本版变更）
- 访问：**完整多用户账号（JWT + users 表 + 按 user_id 隔离）** ✅
